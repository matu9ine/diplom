# Base URL of the Django backend API.
# Local development (Django runs on port 8000):
REACT_APP_API_URL=http://localhost:8000
# On Render this is set automatically by render.yaml (points to the backend service).
